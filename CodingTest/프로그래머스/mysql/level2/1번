SELECT COUNT(*) as FISH_COUNT
FROM FISH_INFO as FI
INNER JOIN FISH_NAME_INFO as FNI
ON FI.FISH_TYPE = FNI.FISH_TYPE 
WHERE FNI.FISH_NAME = 'BASS' or FNI.FISH_NAME='SNAPPER'

/**
INNER JOIN 사용하면 되는 문제

**/